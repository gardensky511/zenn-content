---
title: "Next.js App Routerで外部ライブラリのデータフェッチをメモ化する方法"
emoji: "🦁"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["nextjs", "react"]
published: false
---

## 🌼 はじめに

```typescript:axiosClient.ts
import axios from 'axios';

export const fetchPost = async (id: number) => {
    const { data } = await axios.get(`https://jsonplaceholder.typicode.com/posts/${id}`);
    return data;
};
```

## 1. react cache

react キャッシュでデータフェッチ関数をフェッチする
```typescript:cachedAxios.ts
import { cache } from 'react';
import { fetchPost } from '@/external/axiosClient';

export const getPostCached = cache(async (id: number) => {
  const res = await fetchPost(id);
  const execId = crypto.randomUUID(); // データフェッチする度にユニークなID生成
  return { ...res, id: execId };
});
```

同じ引数で呼び出すコンポーネントを3つ用意

```tsx:ReactCacheA.tsx
import { getPostCached } from '@/feature/reactCache/lib/cachedAxios';

export const ReactCacheA = async () => {
  const a = await getPostCached(1);
  return (
    <>
      <pre>A: {a.title}</pre>
      <div>ID: {a.id}</div>
    </>
  );
};
```
```tsx:ReactCacheB.tsx
import { getPostCached } from '@/feature/reactCache/lib/cachedAxios';

export const ReactCacheB = async () => {
  const b = await getPostCached(1);
  return (
    <>
      <pre>B: {b.title}</pre>
      <div>ID: {b.id}</div>
    </>
  );
};
```
```tsx:ReactCacheC.tsx
import { getPostCached } from '@/feature/reactCache/lib/cachedAxios';

export const ReactCacheC = async () => {
  const c = await getPostCached(1);
  return (
    <>
      <pre>C: {c.title}</pre>
      <div>ID: {c.id}</div>
    </>
  );
};
```

```tsx:page.tsx
export default function Home() {
  return (
    <>
      <h2>1. react cache</h2>
      <ReactCacheA />
      <ReactCacheB />
      <ReactCacheC />
    </>
  );
}
```

## 2. 'use cache'

## 3. Route Handler + fetch


## 🌷 終わり